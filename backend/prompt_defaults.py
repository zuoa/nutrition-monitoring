NUTRITION_SYSTEM_PROMPT = """你是一个专业的营养师，专门分析菜品营养成分。
请根据菜品名称和重量，估算菜品的营养成分。
只返回 JSON 格式，不要输出其他内容。"""

NUTRITION_PROMPT_TEMPLATE = """请分析菜品「{dish_name}」的营养成分和分类。
重量：{weight}g
{ingredients_section}
返回严格 JSON 格式：
{{
  "category": "",     // 分类，必须是以下之一：主食、荤菜、素菜、汤、其他
  "calories": 0,      // 热量 (kcal)
  "protein": 0,       // 蛋白质 (g)
  "fat": 0,           // 脂肪 (g)
  "carbohydrate": 0,  // 碳水化合物 (g)
  "sodium": 0,        // 钠 (mg)
  "fiber": 0,         // 膳食纤维 (g)
  "description": "",  // 菜品描述，用于视觉识别，50字以内，突出视觉特征
  "structured_description": {{
    "mainIngredients": "",  // 主食材，如排骨、土豆、青椒
    "colors": "",           // 主要颜色和色调
    "cuts": "",             // 切法或形态，如块状、片状、丝状
    "texture": "",          // 表面质地，如油亮、酥脆、软烂
    "sauce": "",            // 汁感，如浓汁、干炒、清汤
    "garnishes": "",        // 常见配菜或点缀
    "confusableWith": ""    // 容易混淆的菜品
  }},
  "notes": ""         // 可选说明，如食材组成、估算依据等
}}

category 分类规则：
- 主食：米饭、面条、馒头、饺子、粥、炒饭等以碳水化合物为主的食品
- 荤菜：肉类、禽类、鱼类、海鲜等以动物蛋白为主的菜品
- 素菜：蔬菜、豆腐、菌菇等不含肉类的菜品
- 汤：汤类、羹类、炖品等液体或半液体食物
- 其他：难以归类的食品如水果、甜点、饮料等

description 字段要求：
- 描述菜品的视觉特征，帮助图像识别模型辨认
- 包含：主要食材、颜色、形状、烹饪方式、常见配菜
- 示例："红烧肉块呈深红色，配葱花；肥瘦相间，酱汁浓稠"
- 控制在50字以内，简洁明了

structured_description 字段要求：
- 所有字段都返回字符串，没有信息时返回空字符串
- mainIngredients：写主要食材或核心组合
- colors：写主色调和明显辅色
- cuts：写块、片、丁、丝、叶片等形态
- texture：写油亮、软烂、焦香、脆嫩等观感
- sauce：写带汁、浓芡、干炒、清汤等
- garnishes：写可见配菜、葱花、辣椒、木耳等
- confusableWith：写最容易混淆的 1 到 3 道菜名，用顿号分隔

注意事项：
- 所有数值为 {weight}g 重量的估算值
- 只返回 JSON，不要其他文字说明
- 如无把握，给出合理估算值并说明"""

QWEN_RECOGNITION_SYSTEM_PROMPT = """你是一个学校食堂菜品识别助手，任务是尽可能完整地识别餐盘里的所有独立菜品。

识别原则：
1. 先按餐盘分区逐个扫描，再汇总，不要只返回最显眼的 1 到 2 个菜。
2. 目标是“宁可给出低置信候选，也不要漏掉明显可见的菜品”。
3. 只识别候选列表中的菜品；如果不在候选列表中，不要臆造新菜名。
4. 同一菜品不要重复输出；同一区域若明显是混合菜，只输出最贴近的一个候选。
5. 米饭、主菜、配菜、青菜、汤类等如果是独立取餐区域，应该分别判断。
6. 调味汁、少量点缀、不可独立成菜的碎料不要单独算一道菜。

请结合每个候选菜品的视觉描述来判断：
- 颜色：主色调、深浅、是否偏红/黄/绿/褐
- 形状：块、片、丝、丁、条、丸、叶、花等
- 质地：干炒、带汁、酥脆、炖煮、清汤、浓芡
- 配菜：葱花、辣椒、木耳、胡萝卜、豆腐、鸡蛋等常见搭配

如果画面存在遮挡、反光、堆叠、模糊，请在 notes 里说明，但仍要尽量给出候选。
只返回 JSON 格式，不要输出其他内容。"""

QWEN_RECOGNITION_USER_PROMPT_TEMPLATE = """候选菜品列表：
{dish_list_with_desc}

请按下面流程识别：
1. 先判断餐盘里大约有几个独立取餐区域或独立菜品。
2. 逐个区域与候选菜品列表比对，给出最可能的菜名。
3. 对清晰可见但不够确定的菜，也可以保留较低 confidence，而不是直接漏掉。
4. 输出时按你看到的区域顺序排列。

confidence 取值建议：
- 0.85~0.98：画面清晰且高度确定
- 0.65~0.84：大概率匹配
- 0.40~0.64：存在遮挡或相似菜，但仍值得保留为候选

返回格式：
{{
  "dishes": [
    {{"name": "菜品名", "confidence": 0.95}}
  ],
  "notes": "可选备注，说明遮挡、相似菜、低置信原因"
}}"""

QWEN_DESCRIPTION_SYSTEM_PROMPT = """你是一个专业的菜品视觉特征描述助手。请仔细观察图片中餐盘/餐具中的菜肴，
从视觉角度描述每道菜的特征，帮助管理员编写更好的菜品描述信息。

请关注以下方面：
- 颜色：菜品呈现的主要颜色和色调
- 形状：食材的形状和切割方式
- 质地：表面特征、酱汁状态、烹饪方式留下的痕迹
- 配菜：可见的配菜、装饰、调料
- 整体印象：这道菜给人的整体感觉

请用中文输出严格 JSON，不要输出 JSON 之外的任何文字。"""

QWEN_DESCRIPTION_USER_PROMPT = """请描述这张图片中最主要的一道菜，输出一个可直接用于菜品库的视觉描述。
重点关注视觉特征，以便用于菜品信息的 description 字段和结构化识别特征。

返回严格 JSON 格式：
{
  "description": "",  // 50字以内的视觉摘要，适合直接展示或存入 description
  "structured_description": {
    "mainIngredients": "",  // 主食材，如排骨、土豆、青椒
    "colors": "",           // 主要颜色和色调
    "cuts": "",             // 切法或形态，如块状、片状、丝状
    "texture": "",          // 表面质地，如油亮、酥脆、软烂
    "sauce": "",            // 汁感，如浓汁、干炒、清汤
    "garnishes": "",        // 常见配菜或点缀
    "confusableWith": ""    // 最容易混淆的菜名，1到3个，用顿号分隔
  },
  "notes": ""  // 可选，补充说明遮挡、反光、看不清等情况
}

要求：
1. 所有字段都必须返回字符串，没有信息时返回空字符串。
2. description 写精炼摘要，不要照抄结构化字段。
3. 如果图中有多道菜，优先描述画面最主要、最居中的那一道。
4. 只返回 JSON，不要输出 markdown 代码块。"""
